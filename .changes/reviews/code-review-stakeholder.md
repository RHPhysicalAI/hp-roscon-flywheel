## Review: Stakeholder & Audience Alignment

**Files reviewed:** 5 (`PROJECT-BRIEF.md`, `THOR-TESTING-REUSE.md`, `BUILD-PLAN.md`, `docs/DEMO_RUNBOOK.md`, `DECISIONS.md`) | **Reviewer:** stakeholder-agent

### Critical
- **`DECISIONS.md:1170` (D024) / `PROJECT-BRIEF.md:118`** -- Phase 4.5 (RHEM device plane) was triggered by "an RHEM engineer asked how the demo integrates with RHEM" (`DECISIONS.md:1173`), not by Kelly Switt, the brief's named decision-maker for demo direction (`PROJECT-BRIEF.md:118`). D024's rationale — "the brief's Red Hat value is the governed lifecycle... a model plane that never reaches a managed device undersells it" — is a real and defensible technical argument, but it is the *orchestrator's* reading of the brief, not a recorded conversation with the person the brief assigns that judgment call to. The scope growth is not small: D024 through D114 span 91 of the repo's 114 decisions (~80% of the entire log), all opened in the 24 hours before this review, none referencing Kelly. Front-of-house timing is preserved (Short Cut stays ~4-5 min, `docs/DEMO_RUNBOOK.md:16`), so "tight and reliable" *as experienced by a booth visitor* likely still holds — but "HP wants limited technical scope" (`PROJECT-BRIEF.md:100`) is a scope constraint, not just a runtime constraint, and the backend surface (SNO 4.17→4.19 double upgrade, new Fleet/Catalog/Registry objects, Tekton reinstatement, a new device VM) roughly doubled in the same week the team must also travel and stand up unfamiliar aarch64 hardware. That combination is exactly what "limited technical scope, first ROSCon" was meant to guard against, and no artifact in the repo shows Kelly was told the scope had grown this much.
  **Fix:** Before Sept 20 travel, get an explicit go/no-go from Kelly Switt on Phase 4.5 as demo content (not just as a technical build) and record it as a decision entry (e.g. `D115 — Kelly Switt sign-off on RHEM in-scope for ROSCon`). If sign-off can't be obtained in time, make Full Live (which is where RHEM complexity is load-bearing on stage) explicitly optional and present the Short Cut as the committed deliverable regardless of RHEM's state.

- **`BUILD-PLAN.md:319`, `BUILD-PLAN.md:506`** -- The contingency kit recording is called "non-negotiable" (`docs/DEMO_RUNBOOK.md:122`) and is the fallback for every beat if the desktop, Fury, or venue link fails (`docs/DEMO_RUNBOOK.md:11-14`), yet as of this review it is still unrecorded and unrehearsed — both Phase 4 exit criteria and the Phase 4.5 exit criteria list it as the last open checkbox, gated behind Phase 4.5 finishing (which itself only just closed out today, D112-D114). With the Fury travel window at Sept 20-25 (`PROJECT-BRIEF.md:81`), roughly 11 days remain, and the plan still has a buffer/ride-alongs slot (days 10-12) ahead of the recording session in the sequencing table (`BUILD-PLAN.md:340-352`). If RHEM (Item 1 above) or the buffer slips, the one artifact that protects "tight and reliable" at a venue with "no cluster, no sim, no network" (`docs/DEMO_RUNBOOK.md:13`) may not exist.
  **Fix:** Treat the Short Cut recording session as the next scheduled task, ahead of Phase 4.5 stretch items (E2 Catalog, ride-alongs). If schedule pressure forces a choice, record the kit on the current stable state now and re-record only the Beat 5/6 clips later if RHEM changes further — don't leave the whole kit blocked on RHEM finishing cleanly.

### Warning
- **`PROJECT-BRIEF.md:122-123`** -- Rick Gosalvez (Fury SSH/access coordination) and Manny (HP on-site booth contact) are named once in `PROJECT-BRIEF.md` and never referenced again anywhere in `BUILD-PLAN.md`, `DECISIONS.md`, or `docs/DEMO_RUNBOOK.md`. There is no carry-over row, open question, or decision entry tracking whether Fury SSH access is confirmed for Sept 20-25, or whether on-site logistics with Manny (booth setup, network at the venue, who has physical hands on the box) have been discussed. Given the venue network is explicitly assumed unavailable (`docs/DEMO_RUNBOOK.md:125`) and the whole Fury plan depends on Rick's access window, this is exactly the kind of external dependency that should have a tracked status, not silence.
  **Fix:** Add two explicit rows to `BUILD-PLAN.md`'s carry-overs table (or the inbox): "confirm Fury SSH access window — Rick Gosalvez" and "confirm on-site booth logistics — Manny," each with an owner and target date before Sept 20.

- **`DECISIONS.md:1262` (D026, row 12) / `BUILD-PLAN.md:284`** -- The multi-arch constraint ("build multi-arch from the start," `PROJECT-BRIEF.md:104`) is satisfied in its *end state* (Tekton now builds and signs both arches, D101+, `BUILD-PLAN.md:459-462`), but D026's own drift table records that through most of the build the pipeline hardcoded `cosign-linux-amd64` / `Linux_x86_64` (`pipeline/act_flywheel_pipeline.py:86,105`) and used `:latest` base images despite an earlier decision (D022) saying otherwise. The constraint was honored on paper from the start but not in practice until the final pre-Fury week (Phase 4.5 F, 2026-09-09) — the desktop-only implementation was load-bearing for most of the project's life. This resolved cleanly here, but it's a near-miss worth naming rather than one to read as "no drift."
  **Fix:** No corrective action needed now (already resolved and verified per `docs/eval-records/runtime-image-tekton.md`); capture as a retro lesson — e.g. an early CI smoke build targeting arm64 would have caught the hardcoded-arch assumption in Phase 2 rather than Phase 4.5.

### Suggestion
- **`DECISIONS.md:1328` (D027), `DECISIONS.md:1366` (D028)** -- Model Registry and Catalog (v1alpha1) are additional scope beyond the original phases 0-4, same as RHEM, and share the same pattern: well-reasoned decision records anchored to the brief's language, but no record of a check-in with Kelly Switt. These are individually lower-risk than D024 (both are explicitly marked "removable seam" / stretch), so they don't rise to Critical, but they compound the same underlying gap. Suggest folding them into the same sign-off conversation recommended above rather than treating RHEM in isolation.

- **`docs/DEMO_RUNBOOK.md:64-68`** -- The "Not built (don't claim it)" list correctly excludes the Phase 3+ bootstrap loop, matching `BUILD-PLAN.md`'s unchecked Phase 3+ exit criteria. This is consistent internally, but note that the external "HP status brief" artifact linked from the contingency kit table (`docs/DEMO_RUNBOOK.md:122`) is outside this repo and was not reviewable here — worth a one-time check that it doesn't overstate autonomy/bootstrap-loop claims relative to what `docs/DEMO_RUNBOOK.md` itself allows.

### Positive
- **`docs/DEMO_RUNBOOK.md:50-68, 161-183, 638-653`** -- The "honest framing" constraint (`PROJECT-BRIEF.md:101`) is upheld with real teeth: a "What is true" table with per-claim evidence citations, an explicit "Not built (don't claim it)" list, a "Known screen artifacts (narrate, don't debug)" table for things that look wrong on screen but aren't, and a closing "What's real" / "Pre-baked / shimmed" split that plainly states "the sim is a simulation — no physical arm." This is unusually disciplined for a booth demo script and is exactly what the brief's honesty constraint was asking for.
- **`DECISIONS.md:1262` (D026)** -- The fidelity-drift audit is a strong practice: rather than letting scope/implementation gaps accumulate silently, the team inventoried every place the repo diverged from the reference stack, classified each by cause (legitimate simplification vs. shimmed vs. undocumented drift), and used it to correct the brief's own stale claim about AMQ Streams (`PROJECT-BRIEF.md:47`). Treating the brief as something that gets corrected when reality changes, rather than left stale, is good hygiene.
- **`PROJECT-BRIEF.md:102-103`** -- The two Kelly-specific de-scoping directives (GTC Berlin out of scope, ZGX toolkit de-prioritized) were checked directly against the current repo and found to have zero drift or resurfacing anywhere in `BUILD-PLAN.md` or `DECISIONS.md` — these constraints have held cleanly.

---
**Verdict:** REQUEST_CHANGES
**Summary:** Core constraints (reuse for phases 0-3, single-machine, honest framing, GTC/ZGX de-scoping, eventual multi-arch) hold well, and the project's decision-record discipline is genuinely strong; but Phase 4.5's scope growth was never validated with Kelly Switt despite roughly doubling backend complexity under an explicit "limited technical scope" mandate, and the non-negotiable contingency-kit recording remains undone with under two weeks to Fury travel and open, untracked dependencies on Rick Gosalvez and Manny.
