# Consolidated Review: hp-roscon-flywheel Full Project

**Reviews consolidated:** code-review-security.md, code-review-reliability.md, code-review-domain.md, code-review-platform.md, code-review-quality.md, code-review-stakeholder.md
**Date:** 2026-09-09
**Verdicts:** security: REQUEST_CHANGES, reliability: REQUEST_CHANGES, domain: REQUEST_CHANGES, platform: REQUEST_CHANGES, quality: REQUEST_CHANGES, stakeholder: REQUEST_CHANGES

**Note on this table:** the automated consolidation script silently dropped every finding from the reliability and stakeholder reports (format mismatch — they used `` **`file:line`** `` instead of the required `**[file:line]**`), plus one Critical and all three Warnings from domain (a multi-location bullet broke the same regex). This table was rebuilt by hand from all six source files to recover them. Full detail, exact file:line references, and code excerpts live in the individual `.changes/reviews/code-review-<perspective>.md` files — this table is intentionally compact.

## Summary

- Findings across all reviews: 42 (14 Critical, 19 Warning, 9 Suggestion) after de-duplication (from ~48 raw)
- Two findings independently verified by the orchestrator against live system/host state (marked ⚑ below) before this table was written
- No genuine severity disagreements between reviewers — several findings were independently caught by two reviewers at the same severity (noted inline)

## Triage Required

### Critical (must fix before proceeding)

| # | Finding | Flagged By | Suggested Disposition | Decided |
|---|---------|-----------|----------------------|---------|
| C-1 | ⚑ **RCE on the desktop GPU host.** `edge-kafka-external` NodePort 30903 is PLAINTEXT, no SASL/ACL, reachable off-box. `host_runner.handle()` takes `candidate` straight off the `training-triggers` topic and f-strings it into `docker run --network host --gpus all ... bash -lc`. Orchestrator confirmed both halves by reading the code directly: real command injection, real unauthenticated exposure. | security | **Fix — top priority, before travel.** Drop the NodePort (or add SASL_SSL + a scoped user) and validate `candidate`/`incumbent`/`collector` against a strict charset before they touch a shell string. | _pending_ |
| C-2 | **Curation-gate bypass.** Curator's receiver (NodePort 30802, no auth) builds its write path from an unvalidated `episode_id` — `"../curated/evil"` writes straight into `curated/`, which sync-agent ships to MinIO/Kafka as trusted training data, skipping `score_episode()` entirely. Contradicts the file's own "data never leaves the device without passing curation" claim. | security | **Fix — same priority as C-1.** Validate `episode_id` against a strict charset + path-containment check; drop the NodePort or require a shared-secret header from the emitter. | _pending_ |
| C-3 | ⚑ **`FAILURE_RATE` coin-flip overwrites real ground truth.** `episode_emitter._end_episode()` rolls `random.random() < FAILURE_RATE` (default `0.1`, pinned `"0.1"` in `so-arm-sim.yaml`) and, on a hit, zeroes real `task_success`/`cubes_placed` before the curator ever sees it — directly contradicting the runbook's Beat 2 "scored on ground truth, not a heuristic" claim. Orchestrator checked the live host container directly: `FAILURE_RATE=0` is set there today, so production data is **not** currently being corrupted — but the code default (0.1) and the tracked in-cluster manifest (0.1) are both landmines that fire on any restart without that override. | domain | **Fix.** Delete the injection block or hard-default to 0 and remove the env override entirely. Cheap fix, high reputational risk given the stated audience will read this file. | _pending_ |
| C-4 | **`prune_bags.py --yes` always crashes.** `if not mf:` references an undefined name (should be `manifests_seen`) — introduced in this session's own D113 rewrite. Every `--yes` invocation (run unattended after every dataset assembly) raised `NameError` before deleting anything, the exact disk-hygiene fix the 2026-09-09 outage was supposed to produce. Independently caught by quality and reliability. | quality, reliability | **Already fixed this session** — corrected mid-review, `ast.parse` clean, host copy (`10.0.0.48:~/prune_bags.py`) re-synced. Verify with a real `--yes` dry run before the next rehearsal. | _pending_ |
| C-5 | **`host_runner.py` missing `import sys`.** `sys.exit(...)` on the missing-MinIO-credentials guard raises `NameError` instead of the intended clean message. Independently caught by quality and reliability. | quality, reliability | **Fix.** One line. | _pending_ |
| C-6 | **Missing Argo finalizers, repeat of a paid-for lesson.** None of the 9 `argocd/*-app.yaml` bootstrap Applications carry `resources-finalizer.argocd.argoproj.io`. D075 already hit this exact gap (deleting `act-serving` orphaned its Deployment/Service) and its own text says to check the other apps — never done. | platform | **Fix.** Cheap, and load-bearing for the Fury's fresh-cluster bring-up where apps may need to be deleted/recreated. | _pending_ |
| C-7 | **Fresh-cluster bootstrap will break on the Fury.** `Securesign/securesign` targets namespace `trusted-artifact-signer`, which nothing in git creates. `operators-config-app.yaml` is the only bootstrap app missing `CreateNamespace=true` (which wouldn't even fix this — that flag only creates the app's own destination namespace). | platform | **Fix.** Add a `Namespace/trusted-artifact-signer` manifest at an earlier sync-wave. Test on the desktop now, not during the 5-day Fury window. | _pending_ |
| C-8 | **Floating `:latest` tags, including the core sim producer.** `so-arm-sim.yaml`'s image, both MinIO images, and the local-path-provisioner helper pod are all `:latest`, contradicting the project's own no-`:latest` rule (D022/D112). `so-arm-sim` shares a quay repo with the signed, digest-pinned Tekton output — an unpinned re-pull can silently diverge from what the trust chain enforces. (`manifest-consumer.yaml`'s `python-312:latest` is a separately-tracked, deliberate deferral per D066/D068 — not part of this finding.) | platform | **Fix** for `so-arm-sim` + MinIO + local-path-provisioner. **Defer** the already-tracked `manifest-consumer.yaml` pin (D066/D068's own plan). | _pending_ |
| C-9 | **MinIO root credentials recoverable from git history.** `git show 18c7613:gitops/flywheel/hub-credentials.yaml` still returns them; D078 only reasoned about the live Secret and working tree, not history — and the flightctl `Repository` is declared "public, no auth" so the ResourceSync (and anyone) can clone that history. | security | **Fix.** Rotate now rather than at the Fury rebuild; amend D078 to state plainly whether the repo is actually public. Relevant before wider repo access is shared (Assaf/Sean per inbox notes). | _pending_ |
| C-10 | **Camera health check is presence, not liveness.** `/health` reports `_latest[cam] is not None` — true forever after the first frame ever arrives, even if the sim freezes for the rest of the show. The runbook's Beat 1 recovery flow explicitly trusts this endpoint. Same shape as the tracked D113 gap, but on the demo's opening beat. | reliability | **Fix.** Cheap: stamp with a timestamp, check freshness instead of presence. High visibility payoff (protects Beat 1). | _pending_ |
| C-11 | **D113's eval-gate fix isn't in the deployed image.** The Fleet's pinned runtime digest predates the fix (recorded honestly in D113 itself as "not live"). The exact wedged-action-server scenario that already happened once (D063) would still corrupt a live eval-gate run today. | reliability | **Fix.** Trigger the Tekton rebuild+sign+re-pin before travel (D112 already proved this ~70 min round trip), or explicitly reopen the BUILD-PLAN carry-over as a pre-show blocker rather than "closed." | _pending_ |
| C-12 | **Gripper/wrist joint mixup in four "is the arm settled" checks.** `positions[:5]` assumes arm-chain order; the code's own comment says `/joint_states` is alphabetical, which puts the gripper at index 1 and drops `wrist_roll_joint` entirely. The correct name-based lookup pattern already exists two lines away (`_learn_rest_pose`). Plausible contributor to an already-flagged, previously-undiagnosed loop-breakage. | domain | **Fix.** Apply the existing name-based pattern to all four call sites. | _pending_ |
| C-13 | **No recorded Kelly Switt sign-off on the RHEM scope growth.** Phase 4.5 (D024 onward, ~80% of the entire decision log) was triggered by an RHEM engineer's question, not a conversation with the brief's named decision-maker for demo direction, under an explicit "HP wants limited technical scope" constraint. Front-of-house timing held (Short Cut still ~4-5 min); backend surface roughly doubled. | stakeholder | **Fix — operator action.** Get an explicit go/no-go from Kelly before Sept 20 travel; record it as a decision. If unavailable in time, make Full Live's RHEM-dependent staging explicitly optional and the Short Cut the committed deliverable. | _pending_ |
| C-14 | **Contingency kit still unrecorded, ~11 days from Fury travel.** Called "non-negotiable" in the runbook; every beat's live-failure fallback depends on it; still text/JSON artifacts only, no recordings. Independently flagged as Critical by reliability (technical-fallback-completeness angle) and stakeholder (schedule-risk angle). | reliability, stakeholder | **Fix — operator action, already the acknowledged next task.** Two independent reviewers escalating this to Critical reinforces it belongs at the top of the literal queue, ahead of any further Phase 4.5 stretch work. | _pending_ |

### Warning (should fix)

| # | Finding | Flagged By | Suggested Disposition | Decided |
|---|---------|-----------|----------------------|---------|
| W-1 | `qemu-binfmt` DaemonSet's `nodeSelector: amd64` will never schedule on the all-aarch64 Fury SNO, silently disabling the pipeline's amd64 build leg there. | platform | **Fix** before travel — will actively break on-site, better caught now. | _pending_ |
| W-2 | Kafka runs `privileged: true` + `runAsUser: 0` with no documented rationale, unlike every other privileged workload in the repo. | platform | **Improvement** — investigate/justify or narrow; bundle with C-1's Kafka hardening pass. | _pending_ |
| W-3 | `docker/zenoh-connect.json5` is dead — superseded by inline config generation, nothing references it. | quality | **Improvement** — delete or annotate as historical. | _pending_ |
| W-4 | `dashboard-vllm-reader` Role still grants an unused `services` verb post-act-serving-retirement (its own comment admits this). | quality | **Improvement** — trim, low risk. | _pending_ |
| W-5 | Red Hat AI-assistance marker missing from ~12 files in `src/`/`docker/`, present and consistent elsewhere. | quality | **Improvement** — sweep the list; consider a scanner per the project's own "pair a rule with a check" principle. | _pending_ |
| W-6 | `PROJECT-BRIEF.md`/`AGENTS.md` still say the desktop is `10.0.0.41`; it's been `10.0.0.48` for most of the project's life. Already known/flagged previously. | quality | **Improvement** — trivial two-line fix. | _pending_ |
| W-7 | KFP's `sign_modelcar`/`package_modelcar` download cosign/crane over the network with no checksum, unlike the Tekton path which pins and verifies SHA-256. Runs where the signing key and quay credentials live. | security | **Fix** — the correct pattern already exists in-repo (`cosign-sign-task.yaml`); port it. | _pending_ |
| W-8 | `sign_modelcar` treats a missing Rekor index (`idx[0] if idx else -1`) as success, so a promotion PR can open for an image with no transparency-log entry. Device fails closed at pull time, but surfaces as a confusing stalled rollout at demo time. | security | **Fix** — cheap guard, and add the Tekton path's mandatory verify step. | _pending_ |
| W-9 | `manifest-consumer.py` disables TLS verification entirely on its DSP calls while sending a bearer token with `edit` ClusterRole scope. | security | **Fix** — the correct pattern (`custom_ca=".../service-ca.crt"`) already exists in-repo (`register_model`); same fix applies to `sync-agent.yaml`. | _pending_ |
| W-10 | Device `policy.json`'s `default` transport policy is `insecureAcceptAnything` — enforcement covers only the two named quay repos; a rename/typo/mirror pulls unsigned with no error. D091's negative tests don't exercise the default branch. | security | **Fix** — set `default: reject`, enumerate allowed registries explicitly, add a third standing negative test alongside D091's two. | _pending_ |
| W-11 | `system:openshift:scc:privileged` is bound to the `default` SA cluster-wide in `flywheel`, so every unnamed-SA pod (curator, sync-agent, rejected-mirror, so-arm-sim) inherits it though only Kafka needs it. | security | **Improvement** — scope to a dedicated `edge-kafka` SA via namespaced RoleBinding (the repo's own `pipeline-sa-scc.yaml` shows the right pattern). | _pending_ |
| W-12 | Production (non-eval) coordinator loop has no counter/escalation on repeated goal rejections — same failure signature as the 2026-09-09 outage (D061-D063), silently retries forever. | reliability | **Improvement** — batch with C-11's rebuild; add a rejection counter the dashboard/healthcheck can read. | _pending_ |
| W-13 | Dashboard Deployment has no liveness/readiness probe; a hung Flask process needs a human to notice and manually delete the pod. | reliability | **Improvement** — cheap, standard hygiene. | _pending_ |
| W-14 | The runbook's own recommended "trust the Applications tab over the Fleet banner" fallback is itself sourced from `healthcheck.sh` — inherits the same wedged-server blind spot it's meant to route around. | reliability | **Improvement** — one documentation line clarifying the fallback isn't independent. | _pending_ |
| W-15 | Headline eval numbers (73%→86%, CPU-vs-GPU 18/20 vs 17/20) were measured under different ACT chunking params (30/0.95) than what's actually deployed (100/0.5, D058/D059) — D059 flags this as an open residual; the runbook doesn't disclose it. | domain | **Fix** — re-measure at 100/0.5, or add one disclosure line to the "What is true" table matching the project's own honesty discipline elsewhere (D023, D067). | _pending_ |
| W-16 | `FAILURE_RATE` noise injection (C-3) isn't mentioned as a contributor to the loop-vs-eval success-rate gap the runbook explains by scene-reset differences alone. | domain | **Fix** — becomes moot once C-3 is fixed; until then, add to the same explanation. | _pending_ |
| W-17 | Two independent processes (coordinator, emitter) each poll ground-truth cube pose separately every ~2s and can legitimately disagree; both are treated as authoritative in different places. | domain | **Improvement** — have the coordinator publish its count to the emitter instead of re-deriving independently. | _pending_ |
| W-18 | Rick Gosalvez (Fury access) and Manny (on-site HP contact) are named once in the brief and never tracked again — no status on Fury SSH confirmation or booth logistics. | stakeholder | **Fix — operator action.** Add two tracked rows before Sept 20; cheap, protects the travel-dependency chain. | _pending_ |
| W-19 | Multi-arch ("build from the start") was a near-miss in practice — hardcoded x86 assumptions persisted most of the build, only converging in the final pre-Fury week. Resolved now. | stakeholder | **Dismiss** — already resolved and verified (D101+); capture as a retro lesson only (an early arm64 smoke build would have caught it sooner). | _pending_ |

### Suggestions (improve if approved)

| # | Finding | Flagged By | Suggested Disposition | Decided |
|---|---------|-----------|----------------------|---------|
| S-1 | `task_eval.read_cube_poses()` fails silently to `{}` on any exception, indistinguishable from a real 0/3 in the curator JSON. | domain | **Improvement** — distinguish "sensor unavailable" from "really failed." | _pending_ |
| S-2 | Three manifests use mutable `python:3.12-slim` rather than a digest pin. | platform | **Improvement** — pin alongside the `:latest` cleanup (C-8). | _pending_ |
| S-3 | Policy-window loop logic duplicated between `run_forever()` and `_policy_window()`, already showing minor drift. | quality | **Defer** — real but non-urgent refactor; avoid churn this close to travel. | _pending_ |
| S-4 | Dashboard's hardcoded camera IP (with its own `// TODO`) will break on the Fury's different host IP. | quality | **Defer** — already tracked in the runbook's own comparison table as a known Fury-time gap. | _pending_ |
| S-5 | `THOR-TESTING-REUSE.md`'s `gitops/hub-training/` entry describes thor-testing's layout, not this repo's — could mislead a reader hunting for that path here. | quality | **Dismiss** — technically correct as written, low confusion risk. | _pending_ |
| S-6 | CatalogItem head version (`2.0.0-ft160-rhem`) disagrees with the Fleet's actual current pin (`2.0.0-ft160`) after the rollback — a hand-authored artifact drifted from deployed state. | security | **Fix** — cheap hand-edit; the Catalog is shown to booth visitors as the audit trail, worth being consistent. | _pending_ |
| S-7 | Runtime image built from unpinned `FROM`/`git clone main`/unbounded `pip install` — the signature attests to whatever those resolved to at build time, not a reproducible input set. Also noted: `model-registry.yaml`'s raw ports are a D114-analogue shape, covered by NetworkPolicy but not by port-forward. | security | **Defer** — real supply-chain hygiene gap, but the signature is still valid for what was actually built; post-ROSCon pass, not pre-travel-blocking. | _pending_ |
| S-8 | `disk-guard.sh` is now the *only* backstop against disk fill while `prune_bags.py --yes` was broken. | reliability | **Dismiss** — moot once C-4's fix (already applied) is verified live. | _pending_ |
| S-9 | Model Registry and Catalog (v1alpha1) are additional scope beyond the original phases, same pattern as RHEM (well-reasoned decisions, no recorded Kelly check-in), just lower individual risk. | stakeholder | **Fix — operator action.** Fold into the same sign-off conversation as C-13 rather than treating RHEM in isolation. | _pending_ |

### Positive (no action needed)

- Trust chain (signing, Rekor SET enforcement, negative-trust testing) is genuinely sound where it's applied — security
- D023's "nothing live is promised, contingency-first" plan of record is a mature, disciplined choice for a first-time booth demo — reliability
- Ground-truth pose-based scoring design and the paired seeded-eval statistical methodology (including honestly reporting that some fine-tunes made the policy *worse*) are real statistical discipline, not marketing — domain
- Upstream attribution to `ros-physical-ai/demos` is explicit and generous at exactly the moments a skeptical ROSCon audience would check for a land-grab — domain
- D067's "declared re-release, never call it v3" naming discipline is exactly the kind of self-restraint that keeps the central improvement claim honest under questioning — domain
- Digest-pinning discipline on the Tekton build/sign path (buildah, cosign, qemu-binfmt, base images) is excellent, as is the `helm template` vs. `lookup` handling in `argocd/rhem-app.yaml` and RHOAI sync-wave sequencing — platform
- `gitops/act-serving/`/`src/swap-agent/` retirement is genuinely clean — no live references, only historical doc mentions — quality
- `device/` scripts (provision.sh, enroll.sh, create-vm.sh, bags-share.sh) are consistently strong: idempotent, redacted credentials, actionable errors — quality
- D026's self-audit of product-fidelity drift (including correcting the brief's own stale AMQ Streams claim) is good practice — stakeholder
