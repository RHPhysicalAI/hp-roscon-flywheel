# This project was developed with assistance from AI tools.
# pip install --no-cache-dir --break-system-packages -r requirements.txt   (python 3.12, aarch64, needs git)

# renderer: mujoco-warp 3.13.0 is on PyPI (pure python, tag v3.13.0 = d1a55b6) and needs
# mujoco>=3.12, warp-lang>=1.15. warp-lang 1.17.0 is what already ran inside the MIG slice.
mujoco==3.13.0
mujoco-warp==3.13.0
warp-lang==1.17.0
numpy>=1.26,<3
pillow>=10.4,<13

# SDF -> MJCF world conversion: the fork and rev that upstream's pixi.toml pins (demos @ 4d3564cd);
# the PyPI release 0.1.2 is too old. It also imports Gazebo's `sdformat` and `gz.math` Python
# bindings, which are not on PyPI: they have to come from the ROS / Gazebo install in the image.
sdformat_mjcf @ git+https://github.com/francocipollone/gz-mujoco.git@79de56da1226568605d0aff35fa457aca10006e9#subdirectory=sdformat_mjcf
dm-control==1.0.46
