# Pre-RHEM archive

Real project history, not the current demo's screens. These artifacts document the first governed
run (`07aaf328…`, "run 6") and the promotion PR that flipped the now-retired `gitops/act-serving/`
Deployment pair via a host swap agent (D022, D025, D075). The project has since moved fully to the
RHEM (Red Hat Edge Manager) device-plane path — see `docs/demo-kit/README.md` for what actually
backs the current Short Cut. Nothing here is needed to understand or run today's demo; it's kept so
the project's early governed-pipeline proof isn't lost.
