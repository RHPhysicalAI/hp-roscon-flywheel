# This project was developed with assistance from AI tools.
#
# Bootstrap once: flightctl apply -f rhem/bootstrap/repository.yaml
# (after `flightctl login https://api.flightctl.apps.sno-flywheel.local --token $(oc whoami -t) --insecure-skip-tls-verify`)
#
# flightctl Repository pointing at this repo (public, no auth). Field names per the
# flightctl 1.3 core OpenAPI (GitRepoSpec: type, url).
apiVersion: flightctl.io/v1beta1
kind: Repository
metadata:
  name: hp-roscon-flywheel
spec:
  type: git
  url: https://github.com/RHPhysicalAI/hp-roscon-flywheel.git
